name: Echo

on:
  push:
    branches:
      - "**"

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: actions/checkout@v3

      - name: "Echo"
        run: |
          ls -la .
          ls -la ./.git/refs/tags
